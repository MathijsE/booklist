{% extends 'base.html.twig' %}

{% block title %}Edit Review{% endblock %}

{% block body %}
    <h1>Edit Review</h1>

    {{ include('review/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-info" href="{{ path('review_index') }}">back to list</a>

    <a class="btn btn-danger">{{ include('review/_delete_form.html.twig') }}</a>
{% endblock %}
