{% extends 'base.html.twig' %}

{% block title %}Edit Book{% endblock %}

{% block body %}
    <h1>Edit Book</h1>

    <a class="btn btn-light">{{ include('book/_form.html.twig', {'button_label': 'Update'}) }}</a>

    <a class="btn btn-info" href="{{ path('book_index') }}">back to list</a>

    <a class="btn btn-danger">{{ include('book/_delete_form.html.twig') }}</a>
{% endblock %}
